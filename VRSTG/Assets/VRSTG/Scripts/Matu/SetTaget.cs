using UnityEngine;

public class SetTarget : MonoBehaviour
{
    [SerializeField] float m_target;
    //[SerializeField] 

    // Update is called once per frame
    void Update()
    {
        
    }
}
